/* si dedfinisca una matrice N*N di interi. SI scriva un progamma che determini 
la dimensione massima di una sottomatrice quadrata costituita da elementi uguali.
Il programma deve stampare la dimensione, le coordinate dell'elemento della sottomatrice
posto in alto a sinistra ed il valore degli elementi.
DI seguito si specifica un esempio di input ed output quando N=5, N va fissata a piacere 
dal programma
*/
#include <iostream>
using namespace std;

int mai(){
    int dim;
    cout << "Inserisci la dimensione della matrice: \n";
    cin >> dim;

    int ** M = new int * [n];

    for (int i=0; i<dim; i++){
        M[i] = new int [3]; 

        for(unsigned i=0; i<dim; i++){
            for(unsigned j=0; j<dim;j++){
                cout << M[i][j]<< " ";
                if (j >= 1 && j <= )
            }
        }

    }
}
